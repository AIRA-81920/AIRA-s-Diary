# AIRA's Diary — "提问"（结晶 questioning 阶段）深度解析

> 基于对源码逐行研读 + 逐层追问整理而成。覆盖：功能定位、三层 stage 语义、状态机与路径、prompt 组装管线、LLM 出题逻辑、维度/key/字段关系、以及在与用户讨论中被发现的设计隐患。

---

## 1. 功能定位：提问在系统中处在什么位置

"提问" = 结晶流程的 **questioning 阶段**，是结晶三大环节（感知类型 → 定制化追问 → 生成结晶体）的中间环节。

它不是一张固定表单，而是一条**多轮对话闭环**：AI 出题 → 用户作答 → AI 判断"信息够不够生成结晶"，不够就再问 1 题，够了就出结晶。

关键架构事实：**这套流程是"前端驱动的状态机"，后端 Agent 只提供每个阶段的 LLM 能力，不持有流程状态**。所有状态（题集、答案、会话历史、当前索引）都存frontend Zustand store。

---

## 2. 核心澄清：系统里存在三层互不相同的 "stage"

这是最初最容易混淆的点——同一个词 "stage" 在三个层级各说各话：

| 层 | 谁持有 | 取值 | 语义 |
|---|---|---|---|
| ① HTTP body 层 | 前端 → 后端请求体 | `initial` / `questioning` / `generate_crystal` | 告诉后端"这一轮执行哪个代码分支"（出题 / 判定 / 直接结晶） |
| ② LLM 返回层 | CrystallizeAgent 解析的 JSON | `A` / `B` / `C` | LLM 的裁决：返回题集 / 继续追问 1 题 / 返回结晶 |
| ③ Zustand UI 层 | 前端 `crystallizeStage` | `idle` / `sensing` / `sense_confirm` / `capsule_detection` / `questioning` / `crystal_preview` / `done` | 纯粹决定当前渲染哪个界面 |

典型协作（首轮提问）：
```
crystallizeStage='questioning'（UI） → 发 stage:'initial'（HTTP） → 后端拼 prompt 调 LLM
→ 返回 stage:'A' + 题集（LLM）→ 前端存题集并渲染
```

⚠️ 关键结论：`stage=initial` 是**前端对后端下达的"指令参数"**，不是 LLM 返回的内容。它只决定 `_buildPrompt` 走哪个 if 分支。

---

## 3. 状态机与用户路径全景

### 进入"提问"的 4 条路径（pre-questioning）

`sensing` 之后，按优先级分流（胶囊 > 置信度 > 兜底）：

1. **胶囊介入**：`detected_capsules` 非空 → 先 `capsule_detection`，用户选 use/ignore（优先级最高）
2. **高置信直通**：`confidence ≥ 0.85` → 直接进 questioning（main path）
3. **低置信确认**：`confidence < 0.85` → `sense_confirm` 用户确认/修正类型后进 questioning
4. **失败兜底**：sense 抛错或无 API key → 兜底为"产品想法 / prd"直接进 questioning

### 提问内部的 6 条分支

1. 答题 → 判 `stage C` → 生成结晶 → 预览
2. 答题 → 判 `stage B` → 追加 1 题 → 回问（闭环）
3. 点「跳过，直接生成PRD」 → `stage=generate_crystal` → 直达预览
4. 预览点「重新生成」 → 再生成 → 回预览
5. 补充题（`key='_supplement'`，选做）→ 可空提交 → 照常判定
6. 中途「换类型」→ 清空问答状态 → 用新类型重跑 initial

---

## 4. prompt 组装管线：从用户内容到问题

`_buildPrompt`（initial 分支）并不是用硬编码问题，而是把配置"编译"成一段结构化 prompt，让 LLM 出题。管线如下：

```
用户原文 + inspirationType + 胶囊决策
        │
        ▼
TYPE_DIMENSIONS[类型].required / optional   ← "这个类型该问哪些大维度"
        │
        ▼ (buildDimensionSection 渲染每个维度)
DIMENSION_POOL[维度].definition             ← "这个维度有哪些选项/题型/判断信号"
        │
        ▼
拼接成 prompt：灵感内容 + 类型 + 维度段(含定义/信号) + AI选择规则 + JSON schema
        │
        ▼
BaseAgent.generate() → LLM
        │
        ▼
_parseJSON → stage:'A' + clarifying_questions[]
```

**buildDimensionSection 按题型渲染**（4 种题型）：
- `single_choice` → `### 大维度 X（必选 1 个小维度）` + 每个选项带【定义】【判断信号】
- `multi_choice` → 加 `multi:true, max_select:N`
- `free_text` → 直接给提问方向
- `two_step` → 列出子维度

---

## 5. LLM 出题逻辑（4 步）

题目内容 100% 由 LLM 生成，后端只提供骨架约束：

1. **选维度**：LLM 读原文，拿原文的词去撞每个选项的【判断信号】（语义匹配，不是 `includes`），先在必选维度选小维度，再从可选维度挑 0-2 个，凑够题数。结果记入 `selected_dimensions[]`（含大维度/小维度 id/理由）。
2. **写问题**：按"问题文本质量约束"6 条——最核心是"问题开头须体现'基于你说的 XX...'与原文的连接"，禁止空泛问题。
3. **编选项**：4-5 个"从原文能推断出的具体可能"，不给抽象的"是/否"。
4. **打标签**：每题带 `key`（对应结晶字段名）+ `source_dimension`（选项 id），末尾强制追加 `_supplement` 补充题。

**追问轮（stage='questioning'）只补 1 题**，prompt 明确"一次只问 1 个最关键的问题"，且禁止重复已问过的大维度。

---

## 6. 概念传递链：维度 / 维度池 / key / 结晶字段

### 四个名词的本质

| 名词 | 本质 | 角色 |
|---|---|---|
| 维度 | "该问什么"的分类标签（大维度 + 具体选项） | 概念的第二层身份 |
| 维度池（DIMENSION_POOL） | 查字典：维度名 → 选项/题型/信号 | 把"想知道的维度"翻译成"能问的具体素材" |
| key | 每个问题上的字段名预言 | 桥：把"这题的答案"指向"结晶体的哪个字段" |
| 结晶字段（TYPE_BRANCHES） | 结晶体骨架（base 必有 / dynamic 附加 / meta 推断） | 终点：概念落脚容器 |

### 概念接力（发散 → 收敛）

同一个概念（如"围棋陪练 App"）旅程 = 换 5 次名字/身份：

```
原文（人话形态）
  → 身份① 类型（产品想法）          ← 感知 prompt 分类
  → 身份② 大维度名单                  ← 类型维度配置
  → 身份③ 具体选项 + 信号(A1 应用)   ← 维度池
  → 身份④ 问题 + key                  ← LLM 出题时悬挂 key
  → 身份⑤ 结晶字段 key 对接 → crystal ← 字段骨架 + 回答填值
```

**核心结论（用户讨论确认）**：
- 系统整体是"**发散-收敛**"结构：大维度发散出很多可能性 → key 收敛到类型私有字段骨架。
- **"维度 → key → 字段"之间没有代码级硬映射**——是"语义软对应"，映射由 LLM 在生成时现场裁决。

### 为什么拆开、为什么走 key（不写死映射）

1. **共享**：维度池是"类型无关"的共享题库，一个维度可被多个类型复用（如"视觉风格"）；若写死"维度→字段"，一个维度就只能服务一个类型 → 每个类型都要复制维度 → 池子爆炸。
2. **独立演化**：字段是"按类型私有"的容器，历史上多次改名/废弃（如 `concept_orientation` 移除）；绑定的话改字段要连带改维度池。
3. **让模型裁决映射**：key 由 LLM 同时看到「维度定义 + 字段骨架」后当场决定，属于"有边界的翻译"——代码只保证合法候选集，不负责点对点钉死。

---

## 7. 关键问题与解答（讨论实录提炼）

### Q1：大维度和 key 对应的结晶字段是否有明确的对应关系？否则为什么要拆开还要走 key？

**答：没有硬对应，正是为了不写死。** 三个证据：
- 维度池里没有任何"此维度 → 字段"的配置行；
- 字段骨架独立成表，不反向引用维度；
- key 是 LLM 现场编的（prompt 只说"对应 crystalField 名"，让模型凭语义猜）。

### Q2：既然 key 是 LLM 现场生成的，前端 CRYSTAL_FIELD_META 如何确保匹配到正确的字段？

**答（重要发现）：前端没有"匹配机制"，它是一张"显示白名单"，不是匹配器。**

渲染逻辑（预览阶段）：
```javascript
CRYSTAL_FIELD_META[crystalType].map(field => {
  const rawValue = prd?.[field.key]   // 拿前端的 key，去 LLM 返回的 crystal 对象里取值
  ...
})
```

- LLM 产生的 key 若不在白名单里 → **永不渲染**（值仍在，只是界面不显示空框）
- 匹配的"保证"来自：后端 prompt 用 TYPE_BRANCHES 把字段 schema 喂给 LLM（LLM 照抄 key）+ 前端白名单**人工抄写同名**
- 没有运行时校验、没有"key 不合法 → 重试"分支；真正的保险丝是**用户手动编辑**补录

---

## 8. 研究发现的隐患：TYPE_BRANCHES 与 CRYSTAL_FIELD_META 已发生字段漂移 ⚠️

**这是本次研读中最实质的发现**：后端生成结晶的字段骨架（TYPE_BRANCHES，constants.js）与前端预览白名单（CRYSTAL_FIELD_META，CrystallizePanel.jsx）是**两份各自手抄、无单一数据源**的配置。逐类型对照：

| crystal_type | 后端 TYPE_BRANCHES（LLM 按此生成 key） | 前端 CRYSTAL_FIELD_META（白名单） | 对齐情况 |
|---|---|---|---|
| prd | title/goal/target_user/core_features | title/goal/target_user/core_features/success_criteria | ✅ 基本对齐（前端多 success_criteria，后端 prompt 未定义） |
| scene_card | title/setting/sensory_detail/mood + protagonist/moment | 同 | ✅ 完全一致 |
| worldview | title/premise/rules + inhabitants/tension | title/premise/rules/constraints/inhabitants/tension | ⚠️ 前端多 `constraints`（后端无）→ 预览恒为空 |
| creative_direction | title/**medium**/**theme**/**style_refs** + mood/technique | title/**emotion**/**imagery**/**rhythm**/**theme**/**counterpoint** | ❌ **严重漂移**：仅 title/theme 相符；medium/style_refs/mood/technique 前端不渲染，emotion/imagery/rhythm/counterpoint 取不到值 |
| exploration_map | title/**core_question**/**known_boundary**/**unknown_direction** + method/expected_output | title/**question**/**sub_questions**/**hypothesis**/**methods**/**sources** | ❌ **严重漂移**：仅 title 相符 |
| character_profile | title/archetype/**background**/**personality**/flaw + motivation/relationships/mannerisms | title/**personality**/**background**/motivation/**relations**/**voice** | ⚠️ 中度漂移：archetype/flaw/relationships/mannerisms 前端不渲染；relations/voice 取空 |
| concept_card | title/definition/distinction + origin/signature_features/applicable_context | 同 | ✅ 完全一致 |
| aesthetic_proposal | title/core_definition/aesthetic_attributes/emotional_core/differentiation + cultural_context/signature_elements | 同 | ✅ 完全一致 |
| free_note | title/**content**/**tags** | title/**core_idea**/**trigger**/**desired_form**/**next_step** | ❌ **严重漂移**：仅 title 相符 |

**后果推演**：
- creative_direction / exploration_map / free_note 三类：LLM 生成的正文内容基本**无法在预览中显示**，用户看到大量"待确认（点击编辑）"空框，需手动补录；
- 多出的后端字段（medium、tags 等）值照常写入 `crystal JSON`（不丢失），只是界面上不可见、不可编辑。

**根因**：两份配置无单一数据源，靠人工保持同名；字段系统经历多轮版本演进（概念命题→概念、美学提案新增、方法流程废弃），演变过程中前端表没有同步后端。

**修复建议（如果后续做）**：
1. 最优：前端 CRYSTAL_FIELD_META 从后端 TYPE_BRANCHES 单一来源生成（如后端暴露字段元数据接口，或共享一份 JSON 配置）；
2. 折中：至少对照上表把 5 类漂移字段统一（creative_direction / exploration_map / character_profile / free_note / worldview）；
3. 兜底：预览渲染时对"取不到值"的回退显示原始 JSON 对象，避免信息完全不可见。

---

## 9. 相关代码索引

| 关注点 | 位置 |
|---|---|
| 后端提问引擎（_buildPrompt 三分支） | `backend/src/agents/crystallizeAgent.js` |
| LLM 调用 / JSON 容错解析 | `backend/src/agents/baseAgent.js` |
| 维度池（题库：4 种题型） | `backend/src/config/constants.js` → DIMENSION_POOL |
| 类型 → 维度配置 | `backend/src/config/constants.js` → TYPE_DIMENSIONS |
| 类型 → 结晶字段骨架 | `backend/src/config/constants.js` → TYPE_BRANCHES |
| 前端渲染 / 白名单（已漂移） | `frontend/src/components/CrystallizePanel.jsx` → CRYSTAL_FIELD_META |
| 前端状态机与动作 | `frontend/src/services/store.js`（startCrystallize / _runInitialQuestioning / answerCrystallizeQuestion / skipToPRD / confirmPRD） |
| HTTP 路由 | `backend/src/routes/crystallizeRoutes.js` |
| 控制器 | `backend/src/controllers/crystallizeController.js` |
| 结晶产物文件存储 | `backend/src/services/inspirationStorage.js` |

---

## 10. 附：一句总结

> 概念像旅人：原文出发 → 被分类成类型 → 被展开成维度 → 被问成问题（key 指路）→ 落进字段安家。中途换了 4 次名字，但始终是同一个概念；key 是那张"不论换几次名字，总能找到家"的地图——而这份地图目前部分类型已经画旧了。